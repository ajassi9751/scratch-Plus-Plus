Make a parser
Maybe just use a struct instead of a renderer class ✅
Make the renderer actually efficient and not lock data constantly and not overload cores
Be able to display images
Stratigize how to spread out threads across devices with varying amounts of threads (see planning.txt)
Reorganize the project (like adding an src folder) ✅
Add emojis to commits and other places
Somehow make Cmake automatically retrive the opengl and glfw packages
Create a scratch standard library maybe